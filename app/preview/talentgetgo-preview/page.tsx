import { TalentGetGoLogoScene } from "@/components/preview/talentgetgo-logo-scene";

const page = () => {
  return (
    <main style={{ width: "100vw", height: "100vh", overflow: "hidden" }}>
      <TalentGetGoLogoScene />
    </main>
  );
};

export default page;
