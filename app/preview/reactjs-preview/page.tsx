import { ReactLogoScene } from "@/components/preview/react-logo-scene";

const page = () => {
  return (
    <main style={{ width: "100vw", height: "100vh", overflow: "hidden" }}>
      <ReactLogoScene />
    </main>
  );
};

export default page;
